export default defineAppConfig({
  ui: {
    input: {
      variant: {
        outline: "text-highlighted bg-gray-100 ring ring-inset ring-accented",
      },
    },
    select: {
      variant: {
        outline: "text-highlighted bg-gray-100 ring ring-inset ring-accented",
      },
    },
  },
});
